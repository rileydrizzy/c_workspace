# Makefile for compiling C code using the Clang compiler

# Compiler
CC = clang

# Compiler flags
CFLAGS = -Wall -Wextra -std=c18

# Compile source files to object files
CNR: 
	$(CC) $(CFLAGS) temp.c -o temp && ./temp
