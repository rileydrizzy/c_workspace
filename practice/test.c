#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <float.h>

int main()
{
    printf("The max of INT is => %i\n", INT_MAX);
    printf("The min of INT is => %i\n", INT_MAX);
    printf("The max of long is => %i\n", LONG_MAX);
    printf("The min of long is => %i\n", LONG_MIN);
    printf("The char is => %i\n", CHAR_MAX);

    return EXIT_SUCCESS;
}

